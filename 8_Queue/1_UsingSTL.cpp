#include <iostream>
#include <queue>
using namespace std;

int main()
{
    queue<string> q;
    cout << q.front() << endl;
    cout << "Hello" << endl;
    // q.push(10);
    // q.push(100);
    // q.push(100);
    // q.push(1000);
    // q.push(10000);

    // cout << "Front: " << q.front() << endl;
    // q.pop();
    // cout << "Front: " << q.front() << endl;
    // cout << "Size of queue: " << q.size() << endl;
    // cout << "Queue is empty: " << q.empty() << endl;

    return 0;
}